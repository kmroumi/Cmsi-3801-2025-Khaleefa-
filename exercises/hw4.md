1. no instances: abstract
fixed number of instances: enum
no subclasses: final class
fixed set of subclasses: sealed with permits

2. reference vs value
inheritance
lifecycle
intialization and mutation

3.  it avoids null by using optionals and it prevents this by forcing you to unwrap safely

4. no you cannot use it because of type safety. 

5. void in swift is just a typealias for an empty tuple and its named that to feel familiar to c/obj-c.

6. a supplier is a closure with no parameters that return a value: () -> T

7. he argues that alan kay was wrong to call it object-oriented because the focus should be on objects communicating via messages and not on objects as data holders.

8. class based uses classes as blueprints for creating objects and prototype based creates new objects by cloning existing ones.

9. private final fields
canonical constructor
equals(), hashCode(), and toString() methods
accessors for each field

10. programmers use static methods and fields inside a class 
